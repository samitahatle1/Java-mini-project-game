services:
  - type: web
    name: find-your-bestfriend
    runtime: node
    buildCommand: npm install --prefix server && npm install --prefix client && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production